mkdir dir; touch ./dir/lista; find -type f | cat > ./dir/lista; for ((i = 1; i < `find -type f | wc -l`; i++)); do mv `cat ./dir/lista | head -$i | tail -1` plik$i; done; rm -R ./dir

[pewnie da sie prosciej, ale dziala w wiekszosci przypadkow]
